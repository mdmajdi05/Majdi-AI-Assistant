{
  "name": "majdi-ai-backend",
  "version": "1.0.0",
  "description": "Majdi AI Assistant Backend",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "build": "npm install && if command -v pip3 > /dev/null 2>&1; then pip3 install edge-tts; else echo 'pip3 not available, skipping edge-tts installation'; fi",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "openai": "^4.20.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}